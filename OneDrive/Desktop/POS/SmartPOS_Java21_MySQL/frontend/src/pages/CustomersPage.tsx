import {FormEvent,useEffect,useState} from 'react'
import {api} from '../services/api'
export default function CustomersPage(){const[rows,setRows]=useState<any[]>([]);const[filter,setFilter]=useState('');const[open,setOpen]=useState(false);const[editing,setEditing]=useState<any>();const[form,setForm]=useState({name:'',phone:'',email:'',address:''});const load=()=>api<any[]>('/api/customers').then(setRows);useEffect(()=>{load()},[]);const show=(c?:any)=>{setEditing(c);setForm(c?{name:c.name,phone:c.phone||'',email:c.email||'',address:c.address||''}:{name:'',phone:'',email:'',address:''});setOpen(true)};const save=async(e:FormEvent)=>{e.preventDefault();try{await api(editing?`/api/customers/${editing.id}`:'/api/customers',{method:editing?'PUT':'POST',body:JSON.stringify(form)});setOpen(false);load()}catch(e:any){alert(e.message)}};return <div className="page-stack"><div className="page-actions"><input className="table-search" placeholder="Search customer name, phone, email..." value={filter} onChange={e=>setFilter(e.target.value)}/><button className="btn primary" onClick={()=>show()}>＋ Customer</button></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>Name</th><th>Phone</th><th>Email</th><th>Address</th><th>Loyalty Points</th><th></th></tr></thead><tbody>{rows.filter(c=>`${c.name} ${c.phone||''} ${c.email||''}`.toLowerCase().includes(filter.toLowerCase())).map(c=><tr key={c.id}><td><b>{c.name}</b></td><td>{c.phone||'—'}</td><td>{c.email||'—'}</td><td>{c.address||'—'}</td><td>{c.loyaltyPoints}</td><td><button className="table-btn" onClick={()=>show(c)}>Edit</button></td></tr>)}</tbody></table></div></section>{open&&<div className="modal-backdrop"><form className="modal form-modal" onSubmit={save}><div className="modal-head"><div><small>Customer Database</small><h2>{editing?'Edit Customer':'New Customer'}</h2></div><button type="button" onClick={()=>setOpen(false)}>×</button></div><div className="form-grid"><label className="span2">Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="span2">Address<input value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label></div><div className="modal-actions"><button type="button" className="btn secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="btn primary">Save Customer</button></div></form></div>}</div>}
