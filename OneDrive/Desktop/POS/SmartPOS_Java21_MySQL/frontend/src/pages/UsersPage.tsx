import {FormEvent,useEffect,useState} from 'react'
import {api} from '../services/api'
export default function UsersPage(){const[users,setUsers]=useState<any[]>([]);const[open,setOpen]=useState(false);const[form,setForm]=useState({employeeId:'',name:'',username:'',password:'',pin:'',role:'CASHIER'});const load=()=>api<any[]>('/api/users').then(setUsers);useEffect(()=>{load()},[]);const save=async(e:FormEvent)=>{e.preventDefault();try{await api('/api/users',{method:'POST',body:JSON.stringify(form)});setOpen(false);setForm({employeeId:'',name:'',username:'',password:'',pin:'',role:'CASHIER'});load()}catch(e:any){alert(e.message)}};const toggle=async(u:any)=>{try{await api(`/api/users/${u.id}`,{method:'PUT',body:JSON.stringify({active:!u.active})});load()}catch(e:any){alert(e.message)}};return <div className="page-stack"><div className="page-actions"><div><h3>Users & Role Access</h3><p className="muted">Cashier, Manager and Admin/Owner permissions are enforced in the backend.</p></div><button className="btn primary" onClick={()=>setOpen(true)}>＋ Create User</button></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>Employee ID</th><th>Name</th><th>Username</th><th>Role</th><th>Status</th><th>Last Login</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.employeeId}</td><td><b>{u.name}</b></td><td>{u.username}</td><td><span className={`role role-${u.role.toLowerCase()}`}>{u.role}</span></td><td><span className={`badge ${u.active?'green':'red'}`}>{u.active?'ACTIVE':'DISABLED'}</span></td><td>{u.lastLoginAt?new Date(u.lastLoginAt).toLocaleString():'—'}</td><td><button className="table-btn" onClick={()=>toggle(u)}>{u.active?'Disable':'Enable'}</button></td></tr>)}</tbody></table></div></section>{open&&<div className="modal-backdrop"><form className="modal form-modal" onSubmit={save}><div className="modal-head"><div><small>Admin Only</small><h2>Create Employee Account</h2></div><button type="button" onClick={()=>setOpen(false)}>×</button></div><div className="form-grid"><label>Employee ID<input required value={form.employeeId} onChange={e=>setForm({...form,employeeId:e.target.value})}/></label><label>Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>CASHIER</option><option>MANAGER</option><option>ADMIN</option></select></label><label className="span2">Full Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Username<input required value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label><label>Password<input type="password" minLength={6} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><label>Optional PIN<input value={form.pin} onChange={e=>setForm({...form,pin:e.target.value})}/></label></div><div className="modal-actions"><button type="button" className="btn secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="btn primary">Create User</button></div></form></div>}</div>}
