apiVersion: v1
kind: ConfigMap

metadata:
  name: travel-config

data:
  DB_URL: "jdbc:mysql://mysql-service:3306/travel_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
  DB_USERNAME: "root"
  DESTINATION_SEED_ENABLED: "false"